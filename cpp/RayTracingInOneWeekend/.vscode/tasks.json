{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build main",
            "command": "task",
            "args": [ "build" ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}